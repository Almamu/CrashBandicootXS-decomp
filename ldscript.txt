OUTPUT_ARCH(arm)
SECTIONS
{
	. = 0x08000000;
    ROM :
	ALIGN(4)
	{
        build/crashbandicootxs/asm/crt0.o(.text);
        build/crashbandicootxs/src/main.o(.text);
        build/crashbandicootxs/asm/code.o(.text);

        /* Data */
        build/crashbandicootxs/data/data.o(.data);

	} = 0

    /* Discard everything not specifically mentioned above. */
    /DISCARD/ :
    {
        *(*);
    }
}
